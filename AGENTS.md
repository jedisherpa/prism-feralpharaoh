# Coding Lane: prism-feralpharaoh mainline

Worktree root:

- `/Users/paulcooper/Documents/Codex Master Folder/worktrees/prism-feralpharaoh/mainline`

Lane role:

- repo-wide Prism product changes

Mandatory workflow for substantive coding work:

1. Prism intake.
2. PM plan in `plans/YYYY-MM-DD-<slug>.md`.
3. Agent 1 implementation.
4. Agent 2 architecture refinement.
5. Agent 3 performance and simplicity refinement.
6. PM validation and report in `reports/YYYY-MM-DD-<slug>.md`.
7. Prism final report.

Execution rules:

- inspect the current Prism product code and README before coding
- treat Prism as its own product; do not assume AnythingLLM ownership or metaverse ownership
- use current docs when WebGL, Vite, browser, or dependency details may be stale
- route lab-only work to dedicated Prism lab lanes when appropriate
